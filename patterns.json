{
  "Hashing": [
    {
      "pattern": "MD5",
      "description": "MD5 is a broken hash function and should not be used for security-related purposes.",
      "severity": "High"
    },
    {
      "pattern": "SHA1",
      "description": "SHA1 has known collision vulnerabilities and should be replaced with SHA-256 or stronger.",
      "severity": "High"
    }
  ],
  "Insecure Encryption": [
    {
      "pattern": "RSA.encrypt",
      "description": "Raw RSA encryption is insecure without proper padding (e.g., OAEP).",
      "severity": "High"
    },
    {
      "pattern": "ECB mode",
      "description": "AES in ECB mode is not semantically secure and leaks pattern information. Use CBC, GCM, or CTR.",
      "severity": "High"
    },
    {
      "pattern": "DES",
      "description": "The Data Encryption Standard (DES) is obsolete and vulnerable to brute-force attacks.",
      "severity": "High"
    }
  ],
  "Unsafe Functions (C/C++)": [
    {
      "pattern": "strcpy",
      "description": "strcpy is not buffer-safe and can lead to buffer overflow vulnerabilities. Use strncpy or strcpy_s.",
      "severity": "Medium"
    },
    {
      "pattern": "sprintf",
      "description": "sprintf is not buffer-safe and can lead to buffer overflow vulnerabilities. Use snprintf.",
      "severity": "Medium"
    },
    {
      "pattern": "gets",
      "description": "gets is extremely dangerous as it does not perform bounds checking. It should never be used.",
      "severity": "High"
    }
  ],
  "Insecure Randomness": [
    {
      "pattern": "random()",
      "description": "Standard random number generators are often predictable and not suitable for cryptographic operations. Use a cryptographically secure pseudo-random number generator (CSPRNG).",
      "severity": "Medium"
    }
  ]
}
